/** Nom du cookie de session avocat. Opaque : il ne porte aucune information. */
export const AUTH_COOKIE = 'div_session';

/** Cle sous laquelle AuthGuard depose l'avocat authentifie sur la requete. */
export const AUTH_USER_KEY = 'authenticatedUser';
